//Write a function that recives two numbers and returns their sum.

function sum(a, b) {
	 let total = (a + b)

	console.log(`The total is ${total}`)
}

sum(2, 4)

//The, convert it into an arrwo function

const calcSum = (num1, num2) => {
	let total = (num1 + num2)
	console.log(`The value to two number is ${total}`)
}

calcSum(20, 40)
