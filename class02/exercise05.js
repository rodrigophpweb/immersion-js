/*Create two variables userLoggedIn and userHasPermission. Both should be true.
Check if the user can access the system (only if both are true).*/

let userLoggedIn = true;
let userHasPermission = true;

console.log(`User can access the system: ${userLoggedIn && userHasPermission}`);
