//Check if a person's age is greater than 18. Display the result in the console.

const person = {
    name: "Fernanda",
    age: 30
}

console.log(`${person.name} has ${person.age} years. ${person.name} is of legal age? ${person.age >= 18}`)
